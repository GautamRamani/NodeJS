<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.0.0/css/bootstrap.min.css">
    
</head>
<body class="container" style="margin-top: 50px;">
 {{#messages.success }}
    <div class="alert alert-success" role="alert">{{messages.success }}</div>
    {{/messages.success}}
    {{#messages.error }}
    <div class="alert alert-danger" role="alert">{{messages.error }}</div>
    {{/messages.error}}
       <div class="card"> 
        <div class="card-header">
            <ul class="nav nav-pills w-100">
                <li class="nav-pill active">
                    <a class="nav-link">Users</a>
                </li>
                <li class="nav-pill ml-auto">
                    <a class="nav-link active" href="/users/add">Add User</a>
                </li>
            </ul>
        </div>


         <div class="card-body">        
         {{#data.length}}
        <table class="table">
            <thead>
                <tr>
                    <th scope="col">ID</th>
                    <th scope="col">Name</th>
                    <th scope="col">Email</th>
                    <th scope="col">Password</th>
					<th scope="col">Position</th>
                    <th width="200px">Action</th>
                </tr>
            </thead>
            <tbody>
            {{#data }}
                <tr>

                    <th scope="row">{{id}}</th>
                    <td>{{name}}</td>
                    <td>{{email}}</td>
                    <td>{{password}}</td>
					<td>{{position}}</td>
                    <td>
                        <a class="btn btn-success edit" href="../users/edit/{{id}}">Edit</a>
                        <a class="btn btn-danger delete" onclick="return alert('Are you sure want to delete this record?')" href="../users/delete/{{id}}">Delete</a> 
                    </td>
                </tr>
                {{/data}}
            </tbody>
        </table>
        {{/data.length}}
        
        <!-- if result is empty -->
        {{#!data.length}}
            <p class="text-center">No users found!</p>
        {{/!data.length}}
        </div>
</body>
</html>